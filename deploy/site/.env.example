# ============================================================
# Utterlog 环境变量模板
# ------------------------------------------------------------
# 复制此文件为 .env 并按需修改：  cp .env.example .env
# Docker 用户：在 docker-compose.yml 中直接改 environment 即可
# ============================================================

# ---- 数据库 ----
DB_HOST=postgres
DB_PORT=5432
DB_NAME=utterlog
DB_USER=utterlog
# 留空 — deploy.sh / install.sh 会生成 16 位随机密码写入此处
DB_PASSWORD=
DB_PREFIX=ul_

# ---- API ----
PORT=8080
APP_URL=http://localhost:9260
REQUIRE_PUBLIC_APP_URL=false
# 留空 — deploy.sh / install.sh 会生成 48 位随机密钥
JWT_SECRET=

# ---- 对外端口（生产部署用）----
# 仅绑 127.0.0.1，不占用 80/443；nginx/caddy 反代到这里。
# 部署脚本会自动检测端口占用，被占则顺延到下一个可用端口。
UTTERLOG_PORT=9260

# ---- 应用镜像源 ----
# install.sh 会自动探测 registry.utterlog.io，失败时写入 GHCR 作为稳定源。
# 如需固定某个源，可手动设置：
# UTTERLOG_IMAGE_PREFIX=ghcr.io/utterlog
# UTTERLOG_IMAGE_PREFIX=registry.utterlog.io/utterlog
UTTERLOG_IMAGE_PREFIX=
UTTERLOG_IMAGE_TAG=latest

# ---- 文件存储（local 或 s3）----
STORAGE_DRIVER=local
UPLOAD_DIR=uploads

# S3/R2 配置（STORAGE_DRIVER=s3 时生效）
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=auto
S3_PUBLIC_URL=

# ---- Web 前端 ----
NEXT_PUBLIC_API_URL=/api/v1
INTERNAL_API_URL=http://127.0.0.1:8080/api/v1
